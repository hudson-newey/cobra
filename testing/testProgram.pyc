fn main():;
    message: string = str();

    const MYMESSAGE: string = "this is a test";
    myPrint(MYMESSAGE + message);    print("high");

fn myPrint(text: str):;
    print(text);

# this should trigger a warning;
const myVariable1: string = "foo";
const my_variable2: string = "bar";

main();